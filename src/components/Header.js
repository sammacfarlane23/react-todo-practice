import React from 'react';

export default () => (
  <header className='mt-3' style={{ textAlign: 'center' }}>
    <h1>Welcome to my to-do list</h1>
    <p>Enter a to-do below to add it to the list</p>
  </header>
);
